name: "diw-ut1"

services:
  node-diw:   # Nom del servei
    image: node:22.12   # Imatge de docker que usar
    tty: true        # Manté una terminal activa (per fer feina de forma interactiva)
    stdin_open: true # Permet usar comandes al contenidor
    working_dir: /appdiw   # Carpeta de feina dins el contenidor
    volumes:
      - ./vite:/appdiw  # Monta la carpeta local "programa" a la ruta "/appdiw" del contenidor
    ports:
      - "8082:5173"
  scss:
    # image: node:18
    build:
      context: .
      dockerfile: Dockerfile-scss
    tty: true
    stdin_open: true
    working_dir: /app
    volumes:
      - ./Exercicis:/app

  web:
    image: httpd
    tty: true
    stdin_open: true
    working_dir: /usr/local/apache2/htdocs/
    volumes:
      - ./Exercicis:/usr/local/apache2/htdocs/
    ports:
      - "84:80"
  ticketmaster:
    image: node:22
    tty: true
    stdin_open: true
    working_dir: /appdiw
    volumes:
      - ./ticketmaster_prova:/appdiw
    ports:
      - "8086:3000"

  scssexamen:
    # image: node:18
    build:
      context: .
      dockerfile: Dockerfile-scss
    tty: true
    stdin_open: true
    working_dir: /app
    volumes:
      - ./Examen:/app
