services:
  React:
    image: node:22-bookworm
    tty: true
    stdin_open: true
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "80:5173"
    command: sh -c "npm install && npm run dev -- --host"
    environment:
      - CHOKIDAR_USEPOLLING=true